<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="petroraq_layout" inherit_id="website.layout">
        <xpath expr="//head" position="inside">
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
            <style>
                * { box-sizing: border-box; margin: 0; padding: 0; }
                body { font-family: 'Arial', sans-serif; color: #222; background: #fff; }
                a { text-decoration: none; color: inherit; }
                ul { list-style: none; }
                img { max-width: 100%; display: block; }
                .navbar { background-color: #06264B; display: flex; align-items: center; justify-content: space-between; padding: 15px 40px; position: relative; z-index: 100; }
                .navbar-logo img { height: 55px; }
                .navbar-links { display: flex; align-items: center; gap: 30px; }
                .navbar-links a { color: #fff; font-size: 15px; font-weight: 400; padding: 6px 2px; position: relative; transition: color 0.3s; }
                .navbar-links a.active { color: #e9a23e; }
                .navbar-links a.active::after { content: ""; position: absolute; bottom: -4px; left: 0; width: 100%; height: 2px; background: #e9a23e; }
                .navbar-links a:hover { color: #e9a23e; }
                .quote-btn { background-color: #e9a23e; border: none; color: #fff; padding: 12px 24px; border-radius: 6px; font-size: 14px; font-weight: 600; cursor: pointer; display: inline-flex; align-items: center; gap: 10px; transition: background 0.3s; }
                .quote-btn:hover { background-color: #cc8e34; }
                .navbar-toggle { display: none; background: none; border: none; color: #fff; font-size: 24px; cursor: pointer; }
                @media (max-width: 768px) {
                .navbar { padding: 15px 20px; }
                .navbar-toggle { display: block; }
                .navbar-links { position: absolute; top: 85px; left: 0; right: 0; background: #06264B; flex-direction: column; align-items: flex-start; padding: 20px; gap: 15px; max-height: 0; overflow: hidden; transition: max-height 0.3s ease; border-bottom: 3px solid #e9a23e; }
                .navbar-links.open { max-height: 500px; }
                .navbar-links a { width: 100%; }
                .navbar-links .quote-btn { width: 100%; justify-content: center; }
                }
            </style>
        </xpath>
        <xpath expr="//header" position="replace">
            <header class="o_not_editable">
                <nav class="navbar">
                    <div class="navbar-logo">
                        <img src="/pr_website/static/images/logo.png" alt="PETRORAQ Logo"/>
                    </div>
                    <button class="navbar-toggle" aria-label="Toggle menu" type="button"><i class="fas fa-bars"></i></button>
                    <div class="navbar-links" id="navLinks">
                        <a href="#" class="active">Home</a>
                        <a href="#">About</a>
                        <a href="#">Services</a>
                        <a href="#">Projects</a>
                        <a href="#">Certifications</a>
                        <a href="#">Contact</a>
                        <button class="quote-btn" type="button">Request a Quote <i class="fas fa-arrow-right"></i></button>
                    </div>
                </nav>
            </header>
        </xpath>
        <xpath expr="//body" position="inside">
            <script>
                const toggleBtn = document.querySelector('.navbar-toggle');
                const navLinks = document.getElementById('navLinks');
                if (toggleBtn &amp;&amp; navLinks) {
                toggleBtn.addEventListener('click', () =&gt; {
                navLinks.classList.toggle('open');
                });
                }
            </script>
        </xpath>
    </template>
</odoo>
